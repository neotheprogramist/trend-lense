// import { type ExchangeKey, Exchanges } from '$lib/exchange.js';

// export function load({ params }) {
// 	if (params.exchange in Exchanges)
// 		return {
// 			exchangeName: Exchanges[params.exchange as ExchangeKey]
// 		};
// }
